pre-commit:
  piped: true
  follow: true

  commands:
    verify:
      run: cargo fmt --check && CARGO_BUILD_JOBS=4 cargo check --workspace

    unit:
      run: CARGO_BUILD_JOBS=4 cargo test --workspace --lib --bins
